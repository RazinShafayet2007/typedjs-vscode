# Exclude development files
.vscode/**
.git/**
.gitignore
*.vsix

# Exclude test/verification files
verify_parser.js

# Exclude package lock (not needed for extension)
package-lock.json

# Exclude source maps and TypeScript source
**/*.map
**/*.ts
!**/*.d.ts

# Exclude documentation source (README is fine)
CHANGELOG.md

# Exclude node_modules (bundled with esbuild)
node_modules/**
src/
esbuild.config.js
extension.js
server/
**/*.d.ts
